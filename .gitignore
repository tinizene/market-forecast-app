# Generated by scripts/build-review-doc.js: the whole course, English and translation
# side by side, as static HTML. Deliberately not committed — this repo deploys to
# Vercel as a static site, so a committed review/ would be served publicly, and
# middleware.js hard-blocks /data/course/* precisely because the lesson bodies are the
# paid product. Regenerate it locally and send the files to the reviewer directly.
review/

# Local ledger databases - never commit a book of account
numbers-ledger/*.db
numbers-ledger/*.db-wal
numbers-ledger/*.db-shm
