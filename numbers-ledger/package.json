{
  "name": "numbers-ledger",
  "version": "0.1.0",
  "private": true,
  "description": "Double-entry ledger and agent float model for a cash-and-mobile-money numbers game",
  "license": "UNLICENSED",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test \"test/**/*.test.js\""
  }
}
